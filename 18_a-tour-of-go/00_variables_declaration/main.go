package main

func main() {

	// sum := func(a, b int) int { return a + b }(4, 6)
	// fmt.Println(sum)

	// func swap(a, b string) (string, string) {
	// 	return a, b
	// }
	// fmt.Println(swap("hello", "world"))

	/* Short variable declarations */
	// var x,y,z int = 3,4,5
	// i := 100
	// var a, b, c = true, false, "no!"
	// fmt.Println(a, b, c)
	// fmt.Printf("%T %T %T", a, b, c)

	// var i int
	// var f float64
	// var b bool
	// var s string
	// fmt.Printf("%v %v %v %q\n", i, f, b, s)

	/* Type Conversions */
	// i := 42
	// f := float64(i)
	// s := strconv.Itoa(i)
	// fmt.Printf("%T %T %T", i, f, s)

	/*
	   Constants variable
	   Constants are declared like variables, but with the const keyword.
	   Constants can be character, string, boolean, or numeric values.
	   Constants cannot be declared using the := syntax.
	*/
	// const me = "faisal"
	// const a = 45
	// const b = false
	// fmt.Println(me, a, b)

}
